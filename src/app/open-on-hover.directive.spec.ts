import { OpenOnHoverDirective } from './open-on-hover.directive';

describe('OpenOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
